import {useState} from 'react';
export default function JsonEditor({title,onApply}:{title:string;onApply:(v:any)=>void}){const [txt,setTxt]=useState('{}');const [msg,setMsg]=useState('Experimental: paste JSON overrides and click Use in analysis.');const validate=()=>{try{JSON.parse(txt);setMsg('Valid JSON');}catch(e:any){setMsg(e.message)}};const apply=()=>{try{onApply(JSON.parse(txt));setMsg('Applied');}catch(e:any){setMsg(e.message)}};return <div className='p-3 rounded-xl border border-slate-700 bg-slate-900 space-y-2'><h3>{title}</h3><p className='text-xs text-slate-400'>Experimental: paste JSON overrides and click Use in analysis.</p><textarea className='w-full h-28 bg-slate-950 p-2 rounded' value={txt} onChange={e=>setTxt(e.target.value)}/><div className='flex gap-2'><button className='px-3 py-1 bg-slate-700 rounded' onClick={validate}>Validate JSON</button><button className='px-3 py-1 bg-indigo-700 rounded' onClick={apply}>Use in analysis</button></div><p className='text-xs text-slate-300'>{msg}</p></div>}
