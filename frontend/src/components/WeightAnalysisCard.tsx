import type {AnalyzeResponse} from '../types';
export default function WeightAnalysisCard({weight,data}:{weight:string;data:AnalyzeResponse}){return <div className='p-4 rounded-xl border border-slate-700 bg-slate-900 space-y-2'><h3 className='font-semibold'>{weight}</h3><div>Winner top5: {data.winner_ranking.slice(0,5).map(x=>`${x.athlete} (${x.prob.toFixed(2)})`).join(', ')}</div><div>Top4 top5: {data.top4_ranking.slice(0,5).map(x=>`${x.athlete} (${x.prob.toFixed(2)})`).join(', ')}</div><div>Value winner: {data.value_winner.length} | Value top4: {data.value_top4.length}</div><div className='text-xs text-amber-300'>Warnings: {data.warnings.slice(0,2).join(' | ') || 'none'}</div></div>}
